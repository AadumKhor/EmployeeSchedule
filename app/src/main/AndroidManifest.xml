<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.silentlad.employeemanagement">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".manager.MNotificationsActivity"
            android:label="Employee Notifications"
            />
        <activity
            android:name=".manager.MMainActivity"
            android:label="Manage Schedules"
            android:windowSoftInputMode="adjustPan"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".employee.EMainActivity" />
        <activity
            android:name=".login.LoginActivity"
            android:label="@string/login" />
        <activity
            android:name=".MainActivity"
            android:label="@string/main_label"
            android:screenOrientation="nosensor">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>